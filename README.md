# 100 Mini Projects Challenge
100개의 파이썬 미니 프로젝트를 만들며 성장하는 개발 기록

## 📜 진행 상황
✅ 25/100 완료
📅 목표: 2025년 5월까지 100개 완성  

## 📂 프로젝트 목록

### 1️⃣ 🎸 Band Name Generator  
- **설명:** 사용자의 고향 도시와 반려동물 이름을 입력받아 밴드 이름을 생성하는 간단한 프로그램  
- **사용 기술:** Python, Input Handling, String Manipulation  

### 2️⃣ 💰 Tip Calculator
- **설명:** 총 계산서 금액과 팁 비율을 입력하면, 인원수에 따라 각자 부담해야 할 금액을 계산
- **사용 기술:** Python, Math Operations, f-strings
- 
## 3️⃣ 🏝️ Treasure Island Adventure
- **설명:** 선택에 따라 진행되는 텍스트 기반 어드벤처 게임. 올바른 선택을 하면 보물을 찾을 수 있지만, 잘못된 선택을 하면 게임 오버됩니다!  
- **사용 기술:** Python, Conditional Statements (`if-else`), User Input Handling

## 4️⃣ 🎢 Rollercoaster Ride Checker
- **설명:** 키와 나이를 입력하면 롤러코스터를 탈 수 있는지 확인하고, 나이에 따라 티켓 가격을 계산하는 프로그램 
  - 사진 촬영을 원하면 추가 요금 $3 부과  
- **사용 기술:** Python, Conditional Statements (`if-elif-else`), User Input Handling

## 5️⃣ ✌️ Rock Paper Scissors Game
- **설명:** 사용자가 `가위(2)`, `바위(0)`, `보(1)` 중 하나를 선택하면, 컴퓨터도 랜덤으로 선택하여 승패를 결정하는 게임
  - ASCII 아트를 활용해 가위바위보 그림을 출력합니다.  
  - 컴퓨터와 동일한 선택을 하면 무승부, 이기면 "You win!", 지면 "You lose!" 메시지 출력
- **사용 기술:** Python, `random` 모듈, List Indexing, Conditional Statements

## 6️⃣ 🔐 PyPassword Generator
- **설명:**  
  이 프로젝트는 Python을 활용한 간단한 비밀번호 생성기입니다.  
  사용자로부터 포함할 **문자(letters)**, **숫자(numbers)**, **특수문자(symbols)**의 개수를 입력받아,  
  각 목록에서 랜덤하게 선택한 후 섞어 최종 비밀번호를 생성합니다.

- **사용 기술:**  
  Python, `random` 모듈, 리스트 조작, 사용자 입력 처리

## 7️⃣ 🧭 Maze Solver (Reeborg's World)

- **설명:**  
  이 프로젝트는 [Reeborg's World](https://reeborg.ca/reeborg.html?lang=en&mode=python&menu=worlds%2Fmenus%2Freeborg_intro_en.json&name=Maze&url=worlds%2Ftutorial_en%2Fmaze1.json)에서 제공하는 미로 문제를 해결하는 코드입니다.  
  간단한 조건문과 반복문을 사용해 로봇이 미로를 탈출할 수 있도록 동작을 구현했습니다.  
  - 오른쪽이 열려있으면 오른쪽으로 회전 후 이동  
  - 앞쪽이 열려있으면 이동  
  - 둘 다 막혀있으면 왼쪽으로 회전  
  방식으로 진행됩니다.

- **사용 기술:**  
  Python, 조건문(`if-elif-else`), 반복문(`while`)

- **실행 방법:**  
  1. 위의 [Reeborg's World 링크](https://reeborg.ca/reeborg.html?lang=en&mode=python&menu=worlds%2Fmenus%2Freeborg_intro_en.json&name=Maze&url=worlds%2Ftutorial_en%2Fmaze1.json)로 이동  
  2. 에디터에 코드를 복사하여 붙여넣고 실행하면 미로를 탈출하는 로봇의 움직임을 확인할 수 있습니다.
 
 ## 8️⃣ 🎮 Hangman Game

- **설명:**  
  이 프로젝트는 Python으로 구현한 **행맨 게임(Hangman Game)** 입니다.  
  사용자는 숨겨진 단어를 한 글자씩 추측하여 맞춰야 하며, 틀릴 경우 목숨(lives)이 줄어듭니다.  
  - 모든 목숨이 소진되면 게임 오버!  
  - 단어를 모두 맞히면 승리합니다.  
  - 단어 목록은 `hangman_words.py` 파일에서, 게임 로고와 행맨 그림은 `hangman_art.py` 파일에서 가져옵니다.

- **사용 기술:**  
  Python, 조건문, 반복문, 리스트, 사용자 입력, 모듈 임포트, ASCII Art

  - **모듈 구조:**  
  - **hangman_words.py**: 단어 목록(`word_list`)을 제공  
  - **hangman_art.py**: 게임 로고(`logo`)와 각 목숨 단계에 따른 행맨 그림(`stages`)을 제공

## 9️⃣ 🔄 Caesar Cipher
- **설명:**  
  이 프로젝트는 Python으로 구현한 **Caesar Cipher** 프로그램입니다.  
  사용자가 입력한 메시지를 지정한 만큼의 문자 위치를 이동시켜 암호화(encode) 또는 복호화(decode)할 수 있습니다.  
  프로그램 시작 시, `art.py` 파일에 정의된 로고(ASCII 아트)를 출력하여 멋진 시작 화면을 보여줍니다.
  
- **특징:**  
  - 알파벳 이외의 문자(숫자, 기호, 공백)는 그대로 유지됩니다.
  - 프로그램 종료 후, 재시작할 수 있도록 반복 실행 기능을 제공합니다.

- **사용 기술:**  
  Python, 문자열 조작, 리스트, 조건문, 반복문, 모듈 임포트

## 1️⃣0️⃣ 💰 Secret Auction Program
- **설명:**  
  이 프로젝트는 Python으로 구현한 **비밀 경매 프로그램**입니다.  
  여러 사용자가 입찰할 수 있으며, 최종적으로 가장 높은 입찰 금액을 제시한 사람이 낙찰됩니다.  
  프로그램 시작 시, `art.py` 파일에 있는 ASCII 아트 로고를 출력하여 경매 분위기를 연출합니다.

- **특징:**  
  - 사용자 이름과 입찰 금액을 입력받아 저장  
  - 여러 명이 입찰 가능하며, 입찰이 끝나면 최고 입찰자를 자동으로 선정  
  - 다른 사용자의 입찰 내용을 볼 수 없도록 터미널 화면을 지우는 기능 포함  

- **사용 기술:**  
  Python, 딕셔너리, 조건문, 반복문, 사용자 입력 처리

## 1️⃣1️⃣ 🧮 Simple Calculator
- **설명:**  
  이 프로젝트는 Python으로 구현한 **기본 사칙연산 계산기**입니다.  
  사용자가 입력한 두 숫자와 연산 기호(+, -, *, /)를 이용하여 계산을 수행하며,  
  연속 계산 기능을 지원하여 이전 결과를 이용한 추가 연산이 가능합니다.  
  프로그램 시작 시 `art.py` 파일에 있는 ASCII 아트 로고를 출력합니다.

- **특징:**  
  - 덧셈(+), 뺄셈(-), 곱셈(*), 나눗셈(/) 기능 제공  
  - 연속 계산 기능 지원 (이전 결과값을 이용해 추가 연산 가능)  
  - 새로운 계산을 원할 경우 프로그램이 재시작됨  

- **사용 기술:**  
  Python, 함수, 딕셔너리, 재귀 함수, 사용자 입력 처리

## 1️⃣2️⃣ ♠️ Blackjack Game
- **설명:**  
  이 프로젝트는 Python으로 구현한 **블랙잭(Blackjack) 게임**입니다.  
  사용자가 딜러(컴퓨터)와 함께 21을 목표로 카드를 받고, 점수에 따라 승패를 결정합니다.  
  프로그램 시작 시 `art.py` 파일에 있는 블랙잭 로고(ASCII 아트)를 출력합니다.

- **특징:**  
  - 기본 블랙잭 규칙 적용 (A는 1 또는 11로 계산)  
  - 사용자와 컴퓨터가 번갈아 가며 카드를 받습니다.  
  - 승패는 점수 비교 및 블랙잭 여부에 따라 결정됩니다.  

- **게임 규칙:**  
  - 카드는 2장에서 시작되며, 21을 초과하면 패배합니다.  
  - 딜러는 17 이상이 될 때까지 카드를 받습니다.
  - 블랙잭(카드 2장 합이 21) 시 즉시 승리합니다.  

- **사용 기술:**  
  Python, 리스트, 함수, 조건문, 반복문, 랜덤 모듈

## 1️⃣3️⃣ 🎯 숫자 맞추기 게임 (Number Guessing Game)

- **설명:**  
  이 프로젝트는 Python으로 구현한 **숫자 맞추기 게임**입니다.  
  프로그램이 1부터 100 사이의 무작위 숫자를 생성하고, 사용자가 이를 맞추는 게임입니다.  
  게임 시작 시 `art.py` 파일에 정의된 ASCII 아트 로고를 출력하고, 난이도를 선택할 수 있습니다.  

- **특징:**  
  - 1부터 100 사이의 숫자를 맞추는 게임  
  - **easy** 모드: 10번의 시도 기회  
  - **hard** 모드: 5번의 시도 기회  
  - **정답 확인 힌트 제공:** 입력한 값이 정답보다 **높은지** 또는 **낮은지** 알려줌

- **사용 기술:**  
  Python, 조건문, 반복문, 함수, 난수 생성(`random` 모듈)

- **개선 방향 (직접 쓴 코드(main.py)와 솔루션(main_solution.py)과 비교)**  
1. **함수화 구조 개선:**  
   - 난이도 설정 및 점수 계산 로직을 함수로 분리하여 가독성과 유지보수성을 높일 수 있습니다.  

2. **반복 구조 단순화:**  
   - `while` 루프의 조건을 단순히 `while guess != answer:`로 설정한 후, 함수 내부에서 시도 횟수를 관리하면 코드 가독성이 개선됩니다.  

3. **코드 중복 제거:**  
   - 사용자에게 **Too high** 또는 **Too low** 메시지를 출력하는 부분이 중복되어 있으므로, 이를 함수화하여 중복 코드를 줄이는 것이 좋습니다.
   - 

## 1️⃣4️⃣ 🔥 Higher or Lower Game

- **설명:**  
  이 프로젝트는 Python으로 구현한 **Higher or Lower** 게임입니다.  
  사용자는 두 개의 유명인 또는 브랜드 중에서 누가 더 많은 팔로워를 가지고 있는지 맞춰야 합니다.  
  프로그램 시작 시 `art.py` 파일에서 ASCII 아트 로고를 출력합니다.  

- **특징:**  
  - `game_data.py`에서 제공하는 데이터에서 무작위로 두 계정을 선택하여 비교  
  - 사용자가 더 많은 팔로워를 가진 계정을 맞히면 점수가 올라가며 게임이 계속 진행됨  
  - 오답을 입력하면 게임 종료 및 최종 점수 출력  

- **게임 흐름:**  
  1. **로고 출력:** `art.py`의 `logo` 출력  
  2. **랜덤 계정 선택:** `game_data.py`에서 랜덤한 두 계정을 선택  
  3. **비교 대상 표시:**  
     - A: `이름, 직업(설명), 국가`  
     - B: `이름, 직업(설명), 국가`  
  4. **사용자 입력:** `A` 또는 `B` 입력  
  5. **정답 확인:**  
     - 맞히면 `점수 +1` 및 새로운 비교 대상 생성  
     - 틀리면 게임 종료 후 최종 점수 출력  

- **사용 기술:**  
  Python, 리스트, 딕셔너리, 조건문, 반복문, 랜덤 모듈

- **개선 방향**  
1. **함수 활용 최적화:**  
   - `check_answer()` 함수가 존재하지 않으므로, 솔루션 코드처럼 별도 함수로 분리하면 유지보수에 용이함.  

2. **사용자 경험 개선:**  
   - `print("\n"*20)`과 같이 화면을 지우는 기능을 추가하면, 게임 진행 중 이전 라운드의 정보가 사라져 더 깔끔한 UI를 제공할 수 있음.  

3. **정답 확인 과정 단순화:**  
   - `check_answer()` 함수에서 `if a_followers > b_followers:`와 같이 조건을 단순하게 비교하면 코드가 더 간결해짐.
  
## 1️⃣5️⃣ ☕ 커피 머신 프로그램

- **설명:**  
  이 프로젝트는 Python으로 구현한 **커피 머신 시뮬레이션**입니다.  
  사용자는 **에스프레소, 라떼, 카푸치노** 중 원하는 커피를 선택할 수 있으며,  
  머신은 주문을 처리하고 자원을 관리하며, 결제 시스템도 포함되어 있습니다.  

- **특징:**  
  - 메뉴에 따라 필요한 **재료(물, 우유, 커피)**를 확인하여 충분한지 체크  
  - 사용자에게 동전을 입력받아 결제(쿼터, 다임, 니켈, 페니 지원)  
  - 결제 금액이 부족하면 환불, 충분하면 잔돈 반환 후 커피 제공  
  - **"report"** 입력 시 현재 남은 재료 출력  
  - **"off"** 입력 시 프로그램 종료  

- **게임 흐름:**  
  1. **사용자 입력:** 주문할 커피 선택 (`espresso`, `latte`, `cappuccino`)  
  2. **재료 확인:** 충분한 재료가 있는지 검사  
  3. **결제 진행:** 동전 입력 후 금액 비교  
  4. **커피 제조:** 성공 시 자원 차감 후 제공, 부족하면 오류 메시지 출력  
  5. **반복 실행:** 사용자가 "off" 입력 시까지 계속 주문 가능  

- **개선 방향**
1. **자원 및 수익 관리 추가**  
   - 솔루션 코드처럼 `profit` 변수를 사용하여 **총 수익**을 추적하는 기능을 추가하면 현실적인 시뮬레이션이 가능함.  

2. **반복 구조 최적화**  
   - `while is_on:`과 같이 `True` 값을 가진 변수를 이용하면 **반복 실행 구조가 더 명확해짐**.  

3. **함수 역할 명확화**  
   - `is_transaction_successful()`과 같은 함수를 활용하면 **잔돈 계산 및 결제 결과 관리**가 더 깔끔해짐.  

4. **사용자 경험 개선**  
   - 사용자가 **잘못된 입력**을 했을 때 `"Invalid input"` 메시지를 추가하여 안내 기능을 강화할 수 있음.

## 1️⃣6️⃣ ❓ 퀴즈 게임 (Quiz Game)

- **설명:**  
  이 프로젝트는 Python으로 구현한 **퀴즈 게임**입니다.  
  `data.py`에 저장된 질문 데이터에서 무작위로 문제를 출제하며, 사용자는 `True` 또는 `False`를 입력하여 퀴즈를 진행할 수 있습니다.  
  프로그램은 사용자의 정답 여부를 확인하고 점수를 기록하며, 모든 문제를 완료하면 최종 점수를 출력합니다.  

- **특징:**  
  - `data.py`에 저장된 퀴즈 데이터를 사용하여 무작위 질문 출제  
  - `True/False` 입력을 통해 퀴즈 진행  
  - 정답 여부 확인 후 점수 업데이트  
  - 퀴즈가 끝나면 최종 점수 출력  

- **게임 흐름:**  
  1. **퀴즈 데이터 로드:** `data.py`에서 퀴즈 데이터를 가져와 `question_bank` 리스트에 저장  
  2. **퀴즈 시작:** `QuizBrain` 클래스를 사용하여 문제를 하나씩 출제  
  3. **사용자 입력:** `True` 또는 `False`를 입력하여 정답 선택  
  4. **정답 확인:** 입력된 값과 정답을 비교하여 점수를 업데이트  
  5. **퀴즈 종료:** 모든 문제가 끝나면 최종 점수를 출력  

- **사용 기술:**  
  Python, 클래스(`Question`, `QuizBrain`), 리스트, 반복문, 조건문

## 1️⃣7️⃣ 🎨 터틀 그래픽 - Hirst Dot Painting

- **설명:**  
  이 프로젝트는 Python의 turtle 모듈을 활용하여 Hirst 스타일의 점묘화(Dot Painting) 를 생성하는 프로그램입니다.
  프로그램은 랜덤 색상을 사용하여 10x10의 도트 그리드를 그립니다.

- **특징:**  
  - turtle.Turtle()을 활용하여 그래픽을 그림 
  - colorgram을 이용하여 이미지에서 색상 추출 가능 (현재는 미리 정의된 색상 리스트 사용)
  - random.choice()를 사용하여 랜덤한 색상을 선택 

- **작동 방식:**  
  1. **터틀 초기화:** Turtle() 객체를 생성하고 속도 및 초기 위치 설정
  2. **색상 리스트 설정:** colorgram으로 추출한 색상을 리스트로 저장
  3. **점 그리기:** 100개의 점을 그리면서 랜덤 색상을 적용 
  4. **위치 조정:** 한 줄이 끝날 때마다 터틀의 위치를 조정하여 새로운 줄 시작
  5. **출력:** Screen().exitonclick()을 사용하여 클릭하면 창이 닫히도록 설정
     
- **사용 기술:**  
  Python, turtle 모듈, random, colorgram(이미지 색상 추출)

## 1️⃣8️⃣ 🏁 Turtle Race
- **설명:**  
  이 프로젝트는 Python의 turtle 모듈을 활용하여 터틀 경주 게임을 구현한 프로그램입니다.
  사용자는 6마리의 터틀 중 한 마리의 색상을 선택하여 베팅할 수 있으며, 경주가 시작되면 터틀이 랜덤한 속도로 전진합니다.
  가장 먼저 결승선을 통과한 터틀이 승리하며, 사용자의 선택이 맞았는지 출력됩니다.

- **특징:**  
  - turtle.Turtle()을 활용하여 터틀 객체 생성
  - screen.textinput()을 사용하여 사용자로부터 베팅 입력 받기
  - random.randint(0, 10)을 활용하여 각 터틀이 무작위 속도로 이동
  - 결승선을 통과하면 경주 종료 및 결과 출력

- **작동 방식:**  
  1. **터틀 초기화:** 6마리의 터틀을 각각 다른 색상으로 배치
  2. **사용자 입력:** 사용자가 터틀 색상을 선택하여 베팅
  3. **경주 시작:** 모든 터틀이 랜덤 속도로 이동
  4. **결과 확인:** 가장 먼저 결승선을 통과한 터틀의 색상을 확인하여 결과 출력
     
- **사용 기술:**  
  Python, turtle 모듈, random, 리스트, while 반복문

## 1️⃣9️⃣ 🐍 Snake Game 01
- **설명:**  
  이 프로젝트는 Python의 turtle 모듈을 활용하여 스네이크 게임을 구현한 프로그램입니다.
사용자는 화살표 키(Up, Down, Left, Right)를 사용하여 뱀을 조종할 수 있으며, 뱀이 계속 이동하면서 방향을 바꿀 수 있습니다.

- **특징:**  
  - turtle.Turtle()을 활용하여 뱀의 각 세그먼트(몸통) 생성
  - onkey() 이벤트 리스너를 통해 사용자의 키 입력을 감지
  - self.move()를 반복 실행하여 뱀의 지속적인 이동 구현
  - screen.tracer(0)을 사용하여 부드러운 애니메이션 효과 적용

- **작동 방식:**  
  1. **뱀 초기화:** (0, 0), (-20, 0), (-40, 0)에 세그먼트를 생성하여 뱀의 몸통을 만듦
  2. **이벤트 리스너 활성화:** 사용자의 화살표 키 입력을 감지하여 방향 전환
  3. **뱀 이동:** 함수를 통해 뱀이 지속적으로 이동
  4. **게임 루프 실행:** while game_is_on: 루프에서 화면을 업데이트하며 움직임을 유지
     
- **사용 기술:**  
  Python, turtle 모듈, 클래스(Snake), 리스트, while 반복문

## 2️⃣0️⃣ 🐍 Snake Game 02
- **설명:**  
  Snake Game 01에 더하여, 뱀이 음식을 먹을 때마다 길이가 증가하고 점수가 올라갑니다. 벽이나 자기 자신과 충돌하면 게임이 종료됩니다.

- **특징:**  
  - turtle.Turtle()을 활용하여 뱀의 각 세그먼트(몸통) 생성
  - onkey() 이벤트 리스너를 통해 사용자의 키 입력을 감지
  - 뱀이 음식(Food)을 먹으면 길이가 증가하고 점수 상승
  - 벽이나 자기 자신과 충돌하면 게임 종료 메시지 출력

- **작동 방식:**  
  1. **뱀 초기화:** (0, 0), (-20, 0), (-40, 0)에 세그먼트를 생성하여 뱀의 몸통을 만듦
  2. **이벤트 리스너 활성화:** 사용자의 화살표 키 입력을 감지하여 방향 전환
  3. **뱀 이동:** 함수를 통해 뱀이 지속적으로 이동
  4. **게임 루프 실행:** while game_is_on: 루프에서 화면을 업데이트하며 움직임을 유지
  5. **충돌 감지:** 음식과 충돌하면 뱀의 길이 증가 & 점수 증가 / 벽과 충돌하면 게임 종료 / 자기 자신과 충돌하면 게임 종료
  
- **사용 기술:**  
  Python, turtle 모듈, 클래스(Snake, Food, ScoreBoard), onkey() 메서드, 애니메이션 효과(screen.tracer(0), screen.update())

## 2️⃣1️⃣ 🏓 Pong Game
- **설명:**  
  클래식 퐁(Pong) 게임을 turtle 모듈을 사용하여 구현하였습니다.
두 명의 플레이어가 패들(Paddle) 을 조종하여 공(Ball)을 튕기며 점수를 얻는 방식입니다.

- **특징:**  
  - Paddle, Ball, Scoreboard 클래스를 활용한 객체 지향 프로그래밍(OOP) 적용
  - 키보드 조작(오른쪽 플레이어 → Up / Down, 왼쪽 플레이어 → W / S)
  - 공이 패들에 맞으면 속도 증가 & 방향 변경
  - 공이 벽에 부딪히면 반사, 골대 밖으로 나가면 상대방 점수 증가

- **작동 방식:**  
  1. **패들 & 공 생성:** 양쪽 패들과 중앙의 공을 초기화
  2. **키 입력 감지:** onkey()를 사용하여 방향키 및 w, s 입력 감지
  3. **공 이동:** 게임 루프(while game_is_on)에서 공을 지속적으로 움직임
  4. **충돌 감지:** 공이 벽에 충돌하면 반사/ 공이 패들에 충돌하면 속도 증가 & 방향 변경/ 공이 골대 밖으로 나가면 상대방 점수 증가 후 리셋

- **사용 기술:**  
  Python, turtle 모듈, 클래스(Paddle, Ball, Scoreboard), onkey() 메서드, 애니메이션 효과(screen.tracer(0), screen.update())

## 2️⃣2️⃣ 🚗 Turtle Crossing Game
- **설명:**  
  이 프로젝트는 turtle 모듈을 사용하여 만든 도로 횡단 게임입니다.
플레이어는 화살표 키(↑) 를 사용하여 거북이를 조종하며, 도로를 건너 반대편에 도착하면 다음 레벨로 진행됩니다.
자동차에 부딪히면 게임이 종료됩니다.

- **특징:**  
  - Player, CarManager, Scoreboard 클래스를 활용한 객체 지향 프로그래밍(OOP) 적용
  - 자동차 속도 증가: 레벨이 올라갈수록 자동차 이동 속도가 빨라짐
  - 충돌 감지: 자동차와 충돌 시 게임 종료
  - 점수판 표시: 현재 레벨을 화면에 표시
  - 
- **작동 방식:**  
  1. **거북이 생성:** (0, -280) 위치에서 시작, Up 키를 눌러 앞으로 이동
  2. **자동차 생성:** CarManager를 사용하여 랜덤한 위치에서 자동차 생성
  3. **자동차 이동:** 자동차가 일정한 속도로 왼쪽으로 이동
  4. **충돌 감지:** 자동차와 충돌하면 게임 종료/ 반대편 도착 시 레벨 증가 및 속도 상승

- **사용 기술:**  
  Python, turtle 모듈, 클래스(Player, CarManager, Scoreboard), onkey() 메서드, 애니메이션 효과(screen.tracer(0), screen.update())

## 2️⃣3️⃣ 🐍 Snake Game 03
- **설명:**  
  이 프로젝트는 turtle 모듈을 활용하여 만든 업그레이드된 스네이크 게임입니다.
기존 버전에서 개선되어, 최고 점수 저장 기능이 추가되었으며, 벽이나 자기 몸과 충돌하면 점수가 초기화되지만 게임이 리셋됩니다.

- **특징:**  
  - Snake, Food, Scoreboard 클래스를 활용한 객체 지향 프로그래밍(OOP) 적용
  - 음식을 먹으면 뱀의 길이가 증가하고 점수가 올라감
  - 벽이나 자기 몸과 충돌 시 점수가 초기화되지만, 게임이 재시작됨
  - data.txt 파일을 사용하여 최고 점수(High Score) 저장
  
- **작동 방식:**  
  1. **뱀 생성:** 초기 위치에서 뱀의 몸통을 구성하는 세그먼트 생성
  2. **이벤트 리스너 활성화:** 사용자의 키 입력을 감지하여 방향 전환
  3. **뱀 이동:** 게임 루프(while game_is_on)에서 지속적으로 이동
  4. **충돌 감지:** 음식과 충돌하면 뱀의 길이 증가 & 점수 상승/ 벽이나 자기 몸과 충돌하면 게임 리셋 및 최고 점수 갱신

- **사용 기술:**  
  Python, turtle 모듈, 클래스(Snake, Food, Scoreboard), data.txt 파일을 사용하여 최고 점수 저장 및 불러오기, 애니메이션 효과(screen.tracer(0), screen.update())

## 2️⃣4️⃣ ✉️ Mail Merge Project
- **설명:**  
  이 프로젝트는 Python을 사용하여 자동 초대장 생성을 수행하는 프로그램입니다.
invited_names.txt에 입력된 이름을 활용하여, 각 사람에게 맞춘 초대장을 생성하고 저장합니다.

- **특징:**  
  - starting_letter.txt의 내용을 불러와, 이름만 변경하여 새로운 초대장 생성
  - with open()을 사용하여 텍스트 파일을 읽고, 자동으로 여러 개의 초대장 파일을 생성
  - PLACEHOLDER = "[name]"을 사용하여 각 이름으로 변환
  
- **작동 방식:**  
  1. invited_names.txt에서 이름 목록을 불러옴
  2. starting_letter.txt에서 초대장 템플릿을 읽음
  3. [name]을 해당 사람의 이름으로 변경
  4. ReadyToSend 폴더에 개인 맞춤 초대장 파일 저장

- **사용 기술:**  
  Python, 파일 입출력 (open(), read(), write()), replace()를 활용한 템플릿 문자열 변경, 파일 및 디렉토리 관리

- **파일 구조:**
  /letter_generator
├── main.py                  # 초대장 자동 생성 프로그램
├── /Input
│   ├── /Names
│   │   ├── invited_names.txt    # 초대할 사람들의 이름 목록
│   ├── /Letters
│   │   ├── starting_letter.txt  # 초대장 템플릿
├── /Output
│   ├── /ReadyToSend             # 생성된 초대장 저장 폴더

## 2️⃣5️⃣ 🗺️ U.S. States Game
- **설명:**  
  이 프로젝트는 turtle과 pandas 모듈을 활용하여 만든 미국 주 맞추기 게임입니다.
사용자는 텍스트 입력을 통해 미국 50개 주를 맞히며, 정답을 입력할 때마다 해당 주의 위치에 이름이 표시됩니다.
게임을 종료하면 사용자가 맞히지 못한 주 목록이 states_to_learn.csv에 저장됩니다.

- **특징:**  
  - turtle.Screen()을 활용하여 미국 지도 배경 이미지 표시
  - pandas.read_csv()를 사용하여 미국 주 목록 및 좌표 데이터 로드
  - 사용자가 입력한 주가 정답이면 해당 위치에 이름 표시
  - 사용자가 "Exit"을 입력하면 남은 주 목록을 CSV 파일로 저장
  
- **작동 방식:**  
  1. **미국 지도 표시:** turtle을 사용하여 blank_states_img.gif를 배경으로 설정
  2. **사용자 입력 받기:** screen.textinput()을 사용하여 주 이름 입력
  3. **정답 확인:** 맞힌 주는 해당 좌표에 이름 표시/틀리면 다시 입력 요청
  4. **게임 종료 및 학습 모드:** "Exit"을 입력하면 맞히지 못한 주 목록을 CSV 파일로 저장

- **사용 기술:**  
  Python, turtle 모듈, pandas를 활용하여 CSV 파일 읽기 및 쓰기, turtle.textinput()을 사용한 사용자 입력

- **파일 구조:**
/us_states_game
├── main.py                  # 게임 실행 메인 파일
├── 50_states.csv            # 미국 주 및 좌표 데이터
├── blank_states_img.gif     # 미국 지도 이미지
├── states_to_learn.csv      # 사용자가 맞히지 못한 주 목록 저장 파일


## 📧 Contact
- GitHub: [@JIYUN000000](https://github.com/username)
- Email: brilliantstar120@gmail.com
